# HouseHoldConsumption - Renzo

in the model folder there is a code that will check all the households so we can look for a right time to train and test on. there is also a lstm and xgboost model, but we never really worked further on this because we found another way that would work way better