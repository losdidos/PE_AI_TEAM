# ElectricalPrice - Renzo

in the live folder I made an live prediction animation so it can showcase the working of the model really nice 
I put the final model where i took an joblib from and the code for the live prediction as well


in the model folder is our final XGBoost model that Noland and i worked on and a nueral network model that was not finished because we already had a very good working XGBoost model